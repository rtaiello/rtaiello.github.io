# Instructions to build this repo locally
Install Ruby gems locally to the user (without sudo):

```
bundle install --path vendor/bundle
```

Update Bundler:
```
bundle update
```

Run Jekyll locally with Bundler:
```
bundle exec jekyll serve
```